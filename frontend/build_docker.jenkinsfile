#!groovy

node('node') {
  try {
      stage('Checkout'){
        checkout scm
      }

      stage('Build image') {
      }

      //stage('Scan for errors') {
      //}

      //stage('Test image') {
      //}

      stage('Publish image') {
      }
  }
	catch (err) {
		currentBuild.result = "FAILURE"
        throw err
    }
}

