{
    "private": true,
    "scripts": {
        "debug": "cypress open",
        "test": "cypress run --browser chrome --headless"
    },
    "dependencies": {
        "typescript": "^4.0.3"
    },
    "devDependencies": {
        "cypress": "^5.2.0"
    }
}
